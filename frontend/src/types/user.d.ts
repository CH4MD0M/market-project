interface UserProfileFormData {
  name: string;
  phoneNumber: string;
  address: string;
  zipCode: string;
  password: string;
}

interface UserAddressInfo {
  address?: string;
  zipCode?: string;
  phoneNumber?: string;
}
